# Production profile (example)
# GitHub ucet, ze ktereho se synchronizuji SSH public keys.
GH_USER=mondychan
# Lokalni uzivatel, kteremu se spravuje ~/.ssh/authorized_keys.
USERNAME=root
# Interval v minutach pro systemd timer, jak casto se maji obnovit SSH klice.
INTERVAL_MIN=10
# SSH modul: 1 = pokud chybi SSH server, zkusit jej automaticky doinstalovat.
SSH_AUTO_INSTALL=1
# Docker modul: 1 = spustit hello-world test po instalaci, 0 = preskocit.
DOCKER_HELLO=1
# Webmin port (1-65535), default je 10000.
WEBMIN_PORT=10000
# WireGuard interface name (napr. wg0).
WG_INTERFACE=wg0
# WireGuard: 1 = neptat se na potvrzeni adresy (vhodne pro automatizaci).
WG_CONFIRM=1
# WireGuard test po nasazeni: ask | 0 | 1.
WG_TEST=1
# WireGuard endpoint host/IP a port.
WG_ENDPOINT_HOST=vpn.cocoit.cz
WG_ENDPOINT_PORT=13232
# WireGuard peer public key protistrany (serveru).
WG_PEER_PUBLIC_KEY=UG3ZXlRKMuNkzpsHDrknr7KGu7BTmSANgDvBP6yjSGI=
# AllowedIPs routovane tunelem (mezera nebo carka jako oddelovac).
WG_ALLOWED_IPS="192.168.70.0/24"
# Persistent keepalive v sekundach (0-65535).
WG_PERSISTENT_KEEPALIVE=25
# Volitelne DNS servery pro WireGuard interface.
#WG_DNS="1.1.1.1 8.8.8.8"
# WireGuard lokalni adresa interface tohoto peeru ve formatu IPv4/CIDR (odkomentuj pro non-interactive).
#WG_ADDRESS=192.168.70.20/32
# unattended-upgrades: 1 = povolit automaticky reboot po updatech.
UAU_AUTO_REBOOT=0
# Cas automatickeho rebootu ve formatu HH:MM.
UAU_AUTO_REBOOT_TIME=03:30
# Time sync timezone (modulovy default je CET).
TS_TIMEZONE=CET
# Primarni NTP servery (mezera nebo carka jako oddelovac).
TS_NTP_SERVERS="0.cz.pool.ntp.org 1.cz.pool.ntp.org"
# Zalozni NTP servery.
TS_FALLBACK_NTP="time.cloudflare.com time.google.com"
# 1 = cekat na skutecnou NTP synchronizaci, 0 = nekontrolovat strictne.
TS_STRICT_SYNC=1
# Timeout (sekundy) pro strict cekani na NTP synchronizaci.
TS_SYNC_TIMEOUT_SEC=120
